public Integer fibonacci(Integer n) {
    Integer last = 1;
    Integer current = 1;
    Integer temp = 0;
    for(Integer = 3; i <= n; i++) {
        temp = current;
        current += last;
        last = current; 
    }

    return current;
}