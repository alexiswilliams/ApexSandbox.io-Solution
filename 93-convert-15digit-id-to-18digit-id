public String convert15to18DigitId(String fifteenDigit)
{
    if(String.isBlank(fifteenDigit)) return null;
    if(fifteenDigit.length() != 15) return String.valueOf(-1);
    Id recordId = Id.valueOf(fifteenDigit);
    return String.valueOf(recordId);
}